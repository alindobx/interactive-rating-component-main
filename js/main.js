const circleBtn = document.querySelector(".rating-circles");let submitBtn = document.querySelector(".submit");const selectionCntr = document.querySelector(".selection-content");const selectMain1 = document.getElementById('main-container1');const selectMain2 = document.getElementById('main-container2');let btnID = "";circleBtn.addEventListener("click", (e) => {    btnID = e.target.id;    if(btnID === "1"){        console.log('true')    }else {        function top() {            let createSpan = document.createElement("span");            let starImg = `<img src="images/icon-star.svg" alt="star">`;            createSpan.setAttribute('class', 'stars');            document.getElementById('star-content').appendChild(createSpan);            createSpan.innerHTML = starImg;        }        for (let i = 0; i < btnID - 1; i++) {            top();        }    }    document.getElementById(btnID).disabled = true;});submitBtn.addEventListener('click', ()=>{    console.log(`${btnID} out of 5`);    selectMain1.style.display = "none";    selectMain2.style.display = "block";    selectionCntr.innerHTML = `You selected ${btnID} out of 5`;});